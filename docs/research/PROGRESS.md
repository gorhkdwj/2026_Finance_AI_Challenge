# 금융 현안 발굴 리서치 — 진행 기록

각 Task 종료 시 맨 아래에 섹션을 덧붙인다. 기존 내용은 고치지 않는다.

관련 문서
- 설계: `docs/superpowers/specs/2026-07-20-financial-problem-research-design.md`
- 계획: `docs/superpowers/plans/2026-07-20-financial-problem-research.md`

---

## Task 1 — 검증 스크립트 구축

- **상태:** 완료
- **산출:** `tools/validate_problems.py`, `tools/samples/valid_sample.md`, `tools/samples/invalid_sample.md`, `tools/samples/malformed_sample.md`, `tools/samples/silent_failure_sample.md`, `tools/samples/heading_edgecase_sample.md`
- **수치:** 정상 픽스처 3항목 PASS / 위반 픽스처 HARD 3건 검출. 코드 리뷰 3회를 거치며 픽스처를 5개로 확장하고, 조용한 실패 경로 4건을 수정: (1) 어긋난 ID 헤딩이 오류 없이 조용히 파싱에서 빠지던 것, (2) 어긋난 헤딩 검출 후 `current`가 리셋되지 않아 직전 정상 블록의 필드가 오염되던 것, (3) 닫히지 않은 코드펜스가 파일 끝까지를 "펜스 안"으로 간주해 이후 문제 블록 전체를 조용히 무시하던 것, (4) 한 블록 안에서 같은 필드를 두 번 쓰면 나중 값이 이전 값을 조용히 덮어쓰고 PASS 처리되던 것.
- **판단:** 발산보다 검증기를 먼저 만들어, 에이전트가 스스로 형식을 교정하게 함. 초기 구현에는 "오류 없이 조용히 넘어가는" 경로가 여러 개 남아 있었고, 이런 경로는 발산 에이전트 6개가 실제로 형식을 어겼을 때 검증기가 이를 놓치고 PASS를 내주는 결과로 이어지므로, 코드 리뷰를 반복해 조용한 실패를 능동적으로 찾아 막는 방식으로 진행함.
- **다음 Task에 넘기는 것:** 근거 URL은 쉼표 구분 다중 값을 허용함. 코드펜스(```) 안의 내용은 파싱에서 완전히 제외되므로 형식 예시는 코드펜스로 감싸면 안전함. 단, 코드펜스는 반드시 닫아야 하며 닫히지 않으면 HARD 오류로 검출됨. ID 헤딩은 `### P{1-6}-{2자리}` 형식을 엄격히 요구하며, 자리수 부족(`### P1-1`)이나 부가 텍스트(`### P1-01: 제목`)는 HARD 오류로 검출됨. 한 블록 안에 같은 필드를 두 번 쓰면 HARD 오류로 검출됨.

## Task 2 — 리서치 헌장 작성

- **상태:** 완료
- **산출:** `docs/research/00-research-brief.md`, `docs/research/prompts/divergent-template.md`
- **수치:** 헌장 101줄 (상한 140줄)
- **판단:** 브리프의 헌장 본문과 프롬프트 템플릿 내용은 그대로 옮겨 적었고, Task 1에서 추가로 확정된 검증기 동작(어긋난 ID 헤딩 검출, 블록 내 필드 중복 검출) 두 줄만 §8에 덧붙였다. 헌장을 실제 검증기(`tools/validate_problems.py`)로 직접 돌려 `검사 대상: 0개 항목` / `PASS`를 확인함으로써, 헌장 안의 형식 예시(§4)와 요약 블록 규약(§8)이 검증기에 의해 실제 문제 항목으로 오인되지 않음을 실증했다. 이는 문서만 읽고 "형식이 맞겠지" 추정하는 대신, 다음 단계에서 6개 발산 에이전트가 동시에 이 헌장을 프롬프트에 삽입해 쓸 때 자기 파일이 아니라 헌장 자체 때문에 검증이 오염될 위험을 사전에 차단하기 위함이다. 이후 리뷰에서 Critical 1건·Important 4건·Minor 3건이 지적되어 모두 반영했다: (1) 템플릿이 §8 요약 블록 작성 지시를 실행 지시("반드시 지킬 것"·"산출 방법")에서 빠뜨려 검증기가 잡지 못하는 누락 위험이 있었던 것을 세 곳(반드시 지킬 것 1번, 산출 방법 전체, 자체 검증 말미)에 명시적으로 추가해 막음, (2) §4 `당사자` 금지 예시가 "소상공인" 한 단어뿐이라 검증기 `VAGUE_ACTORS` 11개 목록과 불일치했던 것을 목록 전체로 맞춤, (3) §7 공고 예시 7개의 해결책 오염 위험이 P1 축 서술과 §7 경고문 양쪽에서 약했던 것을 양쪽 다 강화, (4) §5 규칙 4(개수 채우기)가 근거 품질을 밀어낼 유인을 만들던 것을 "근거 없는 항목으로 채우지 말 것" 규칙으로 보완, (5) 이 문장 자체의 자기모순(항목 3개를 나열하며 "두 줄"이라 칭했고, 실제로는 §8에 없는 "코드펜스 닫힘 필수"를 있다고 잘못 기록한 것)을 바로잡음. Minor 3건(템플릿 플레이스홀더 개수 5→6 정정 및 `{{RESEARCH_BRIEF}}` 목록 포함, §4 ID 형식에 축번호 범위 명시, §4 "필드 순서를 바꾸지 말 것"을 자동 검증 대상이 아님을 밝히는 문구로 완화)도 함께 반영했다.
- **다음 Task에 넘기는 것:** 헌장(`00-research-brief.md`)과 템플릿(`divergent-template.md`) 모두 검증기로 실측 검증되어 `검사 대상: 0개 항목` / `PASS` / `exit=0`임을 확인함 — 두 문서를 발산 에이전트 프롬프트에 그대로 삽입해도 검증기가 문서 내 예시를 실제 문제 항목으로 오인하지 않는다. 다음 Task(발산 에이전트 실행/오케스트레이션)에서는 `divergent-template.md`의 플레이스홀더 6개(`{{AXIS_ID}}` `{{AXIS_NAME}}` `{{AXIS_SCOPE}}` `{{MIN_COUNT}}` `{{OUTPUT_PATH}}` `{{RESEARCH_BRIEF}}` — 앞 5개는 축별 값, `{{RESEARCH_BRIEF}}`는 전 축 공통 헌장 전문)를 채워 6개 축(P1~P6, §6 표 기준 최소 산출 개수 상이)에 대해 서브에이전트를 병렬 실행하면 된다. 각 에이전트는 산출 후 `python tools/validate_problems.py {산출파일} --min {최소개수}`로 자체 검증하도록 템플릿에 이미 지시되어 있다. 여기에 더해 `--check-urls`를 **권고 절차(게이트 아님)**로 추가했다 — 형식 검증은 URL 실재를 보지 않으므로 환각 URL이 PASS를 받을 수 있으나, 공공기관 사이트의 봇 차단(403)으로 인한 오탐도 흔하므로 자동 탈락시키지 않고 수동 확인하도록 지시한다.

## Task 3 — 발산 축 P1~P3

- **상태:** 완료
- **산출:** `docs/research/01-divergent/p1-notice-reverse.md`, `p2-industry.md`, `p3-persona.md`
- **수치:** P1 20개 / P2 21개 / P3 21개 (합계 62개), 스키마 검증 3파일 전부 PASS(exit=0), 요약 블록 위치 전부 정상(3행 < 첫 문제 블록 10행)
  - P2 업권 분포: 은행 5 / 인터넷은행 4 / 증권 4 / 생보 4 / 보안 4
  - P3 페르소나 분포: 청년 5 / 고령층 3 / 장애인 4 / 외국인 3 / 창업자 3 / 자영업자 3
- **판단:** WARN 1건(P2-18 '에이전트')은 삭제하지 않고 유지했다. 해당 항목에서 'AI 에이전트'는 제안하는 해결책이 아니라 **공격받는 대상**(금융회사가 도입한 AI 에이전트가 고도화 공격에 노출됨)을 가리키는 명사다. 검증기의 `SOLUTION_KEYWORDS`가 단순 부분문자열 매칭이라 발생한 예상된 오탐이며, 이 한계는 Task 1 리뷰에서 이미 Minor로 기록되어 있다. 당사자 뭉뚱그림 WARN은 3축 전부 0건 — 헌장 §4에 검증기 `VAGUE_ACTORS` 11개 목록을 그대로 명시한 조치가 효과를 냈다.
- **다음 Task에 넘기는 것:**
  - P4~P6 실행 시 프롬프트 수정 필요 사항 **없음**. 헌장·템플릿이 의도대로 작동했다(3축 모두 자체 검증을 통과한 상태로 반환).
  - 근거 강도가 약해 Task 6(실재성 검증)에서 우선 확인이 필요한 항목: **P1-06**(고령층 보이스피싱 피해비중 — 지역 매체 인용, 원출처는 2025년 9월 국회 행안위 공개 경찰청 자료), **P3-15**(모기지신용보험 차별 — 2010년 인권위 결정으로 통계가 오래됨), **P3-16·P3-17**(자금조달 통계 — 1차 출처 원문 대조 미완).
  - P3-10(kca.go.kr)은 SSL 인증서 체인 오류로 `--check-urls` 자동 판정에 실패했으나 curl로 HTTP 200 및 본문 일치를 확인했다. Task 6에서 같은 현상이 재발할 수 있으니 자동 실패를 곧바로 탈락으로 처리하지 말 것.

### Task 3 리뷰 후속 (근거 실증)

- **리뷰 방식:** 표본 26건의 근거 URL을 실제로 열어 수치·연도를 대조. 불일치가 나온 축(P1·P2)은 표본을 추가 조사.
- **발견:** Critical 4건 + Important 3건 + Minor 1건. 전부 **근거 URL에 없는 수치를 인용하거나 시점이 어긋난** 유형.
  - P2-05: 판매잔액 15.4조원·오프라인 94.4%가 인용 기사에 없음 → 금융위 보도자료(2025-02-26)의 검증된 수치(손실확정 17만건/원금 10.4조원/손실액 4.6조원/평균배상비율 31.4%)로 **수치 자체를 교체**
  - P2-07: 실제 시점 2022년 하반기를 2025년 하반기로 표기 → 정정
  - P2-08: 대손충당금 적립률 320.8% → 실제 229.58%로 정정
  - P1-16: 근거가 금감원 **게시판 목록 페이지**(시간이 지나면 내용이 바뀜) → KDI 고정 상세 페이지로 교체 + 문제 한줄 템플릿 준수로 재작성
  - P1-13: 장기 추이 수치 출처 보강(다중 URL) / P1-01: 기업 블로그 → 중기부 창업기업실태조사로 격상 / P2 요약 블록 정정
- **재검증:** 8건 전부 URL 재열람하여 수치 일치 확인. p3-persona.md 무변경, 범위 외 변경 없음, 해결책 누출 없음. **Quality 승인.**
- **판단:** 근거 실증을 표본으로만 했는데도 26건 중 8건(31%)에서 문제가 나왔다. 발산 에이전트가 "URL을 열어보고 인용하라"는 지시를 받고도 수치를 부정확하게 옮겼다는 뜻이다. **Task 6(실재성 검증)에서 생존 항목 전건에 대해 URL 내용 대조를 반드시 수행해야 한다.** 형식 검증(`--check-urls`)은 페이지 존재만 보고 내용 일치는 보지 않으므로 자동화로 대체할 수 없다.
- **잔여 Minor 2건:** P2-08 기업대출 비중의 시점 표기가 기사 맥락에서 유추된 것(오류 아님), P1-01 출처 페이지에 2021·2022년 통계가 나란히 있어 후속 검증 시 혼동 소지.
- **Task 5(통합)로 넘기는 중복 후보:** P1-06↔P3-06(동일 URL), P1-19↔P2-18(동일 URL), P1-08↔P2-21, P1-13↔P2-04, P1-09↔P3-17, P1-20↔P2-19, P1-10↔P3-16, P2-05↔P3-07. P3-01(전세사기, 업권 무관)은 금융 범위 경계 사례로 포함 여부 재검토 필요.
- **축별 커버리지 쏠림:** P1은 공고 예시5(포용금융) 5개로 편중, **예시1(창업기업·소상공인 매칭)이 1개뿐**으로 가장 얇음. P2·P3 분포는 균형적.

## Task 4 — 발산 축 P4~P6

- **상태:** 완료
- **산출:** `docs/research/01-divergent/p4-channel.md`, `p5-regulator.md`, `p6-loss-stats.md`
- **수치:** P4 12개 / P5 17개 / P6 12개, 6축 합계 103개, 스키마 3파일 전부 PASS, 요약 블록 위치 정상, WARN 0건, P6 수치없음 0건
  - P4 채널 분포: 창구2 / 모바일앱2 / 인터넷뱅킹2 / 콜센터2 / 오픈뱅킹2 / 마이데이터2
  - P5: 감독당국(금융위·금감원·금융보안원) 공식 지목 문제 17개
  - P6 영역 분포: 보이스피싱3 / 보험사기2 / 이상거래2 / 불완전판매3 / 디지털소외2
- **판단:** Task 3의 근거 불일치 교훈(표본 31% 불일치)을 P4~P6 프롬프트에 4가지 실패 유형과 함께 명시했고, 세 에이전트 모두 수치 인용 시 원문 자리별 재대조를 수행했다고 보고했다. **WebSearch 도구 한도가 세션 공유로 소진되어** 세 에이전트가 curl 기반 직접 크롤링(DuckDuckGo HTML + 원문 fetch)으로 우회했다. 이 방식은 오히려 검색 스니펫이 아니라 원문 HTML을 직접 확인하게 만들어 정확도에 긍정적이었다.
- **다음 Task(5 통합)에 넘기는 것:**
  - P6-06·P6-07(카드사 해킹/유출)은 금감원 제재 절차가 2026.7 기준 진행 중이라 1차 출처 대신 연합뉴스 기사를 근거로 사용 — Task 6 검증 시 확인 필요.
  - P4-04·P4-08·P4-10·P4-12는 근거 시점이 2022~2023년 자료로 최신이 아님 — Task 6에서 신선도 확인 대상.
  - 예상 중복(6축 전체 관점): 보이스피싱 관련 항목이 P1·P3·P6에 분산(P1-06/P1-08/P3-06/P6-01~03), 디지털 소외가 P1·P3·P4·P6에 분산, 보험사기가 P1-16·P6-04~05에 분산. Task 5에서 대규모 병합 예상.

### Task 4 리뷰 후속 (근거 실증)

- **리뷰 방식:** 41개 중 39개 근거 URL을 curl로 원문 대조(P4 12/12, P5 17/17, P6 11/12). 수치를 자리별로 대조.
- **핵심 결과:** **불일치 0건.** Task 3의 31% 불일치가 이번엔 재현되지 않음 — 프롬프트에 4가지 실패 유형을 명시하고 원문 재대조를 강제한 효과. 발산 품질이 축을 거듭할수록 개선됨.
- **발견·수정 (Important 3건):**
  - P6 요약 블록의 "검산 완료" 주장이 사실 오류(14,707명은 6개 연령대 전체 합인데 P6-01·P6-02 2개 항목 합으로 오인) → 정정
  - P1-06 문제 한줄(21%p)과 피해 규모(15%p) 연도·수치 모순 → 원문(2023년 32%→2025년 1분기 53%)에 맞게 정정
  - P5 기관 쏠림(금융위 82%) → 금감원 2건(P5-18 변종 차량담보대출 소비자경보, P5-19 소비자보호 실태평가) + 금융보안원 1건(P5-20 망분리 우회 RED IRIS 리포트) 추가로 완화
- **재검증:** P5 신규 3건 URL 재대조 전부 일치. 6축 합계 **106개**, 스키마 PASS, P6 수치없음 0.
- **Minor(미수정, Task 5·6으로 이월):** P6-06 URL별 수치 표기 명확화, P5-20은 원문 요약본만 공개돼 수치없음(추측 금지 준수).
- **Task 5(통합)로 넘기는 교차 중복 (리뷰어 확정):**
  - 보이스피싱 고령층: **P1-06 = P3-06(동일 URL) ↔ P6-02**
  - 디지털 소외: P1-13 ↔ P3-08 ↔ P4-01·P4-02·P4-05 ↔ P6-11·P6-12
  - 보험사기: P1-16 ↔ P5-03·P5-04 ↔ P6-04·P6-05
  - 장애인 금융접근성: P1-14·P1-15 ↔ P3-09~P3-12 ↔ P4-04·P4-06
  - 외국인 금융접근성: P1-02·P1-03 ↔ P3-13~P3-15 ↔ P5-13
  - 마이데이터: P4-11·P4-12 ↔ P5-02
  - 내부 중복(동일 사안): **P5-03 ↔ P6-04**(동일 URL·수치, 프레이밍만 다름), P6-11 ↔ P6-12(근접)
- **판단:** 교차 중복이 매우 많다. 이는 결함이 아니라 **여러 축에서 독립적으로 도출된 문제가 실재성이 높다는 신호**(헌장 §6에 명시). Task 5에서 중복 횟수를 정확히 기록해 Task 8 가점 근거로 보존해야 한다.

## Task 5 — 문제 풀 통합 및 중복 제거

- **상태:** 완료
- **산출:** `docs/research/02-problem-pool.md`
- **수치:** 원본 106개 → 병합 → **최종 91개**. 중복 횟수 분포: 1회 79건 / 2회 11건 / 3회 1건(P1-06) / 4회 1건(P1-13). 중복 횟수 합=106(원본 전원 반영), 스키마 PASS.
- **판단:** 병합 규칙("당사자 같거나 포함관계 + 손해 종류 동일")을 엄격 적용해 과잉 병합을 피했다. 그 결과 91개로 브리프 권장(40~70)을 초과하지만, 브리프가 "60개 초과 시 Phase 3에서 필터"를 허용하므로 진행. 같은 주제군(외국인 접근성, 마이데이터 등)이라도 손해 메커니즘이 다르면 분리 유지했다.
- **리뷰 후속 (병합 판단 정밀 검증):**
  - 검산: 원본 106개 전원이 대표 또는 중복 출처로 정확히 1회씩 추적됨(Python 정밀 검산). 중복 횟수 필드 불일치 0건, 대표 자기참조 0건.
  - 리뷰 발견 Important 3건 전부 조정: **P6-11↔P6-12 분리**(80세 이상 디지털 회피 vs 일반 소비자 보안우려 병행 — 당사자·손해 상이), **P4-10↔P4-12 병합**(동일 URL·동일 해지 불편 — 놓친 병합), **P1-09+P3-17 병합 유지**(손해 메커니즘 동일: 재무제표 중심 심사로 미래가치 미반영). Minor: 중복 출처에서 대표 ID 자신을 제외하는 규칙을 전수 통일(자기참조 10건 정리).
- **다음 Task(6 검증)에 넘기는 것:**
  - 우선 검증 대상(근거 약함): P1-06(문제한줄-피해규모 수치 이미 Task4에서 정정), P6-06·P6-07(카드사 제재 진행 중, 언론 근거), P5-20(RED IRIS 수치없음).
  - 검증 시 URL 내용 대조는 자동화 불가 — Task 3에서 표본 31% 불일치가 났으므로 생존 후보 전건 대조 필수.
  - 중복 횟수 2+ 항목 13건은 Task 8 가점 근거이므로 검증 과정에서 대표 항목이 탈락하면 중복 출처 중 살아있는 항목으로 대표를 승계시킬 것.
